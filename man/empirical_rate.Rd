% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical_rate.R, R/methods.R
\name{empirical_rate}
\alias{empirical_rate}
\alias{print.empirical_rate}
\alias{summary.empirical_rate}
\alias{print.summary.empirical_rate}
\title{Empirical estimation of marginal event rates and rate ratios}
\usage{
empirical_rate(
  data,
  treatment,
  count,
  exposure,
  covariates = NULL,
  car_scheme = c("simple", "permuted-block", "pocock-simon", "biased-coin", "urn"),
  strata = NULL,
  reference = NULL,
  alpha = 0.05
)

\method{print}{empirical_rate}(x, digits = 4, ...)

\method{summary}{empirical_rate}(object, ...)

\method{print}{summary.empirical_rate}(x, digits = 4, ...)
}
\arguments{
\item{data}{A data frame containing one row per subject.}

\item{treatment}{Character scalar. Name of the treatment-group column in
\code{data}. It is coerced to a factor; the order of its levels determines the
direction of pairwise rate ratios (see \emph{Value}).}

\item{count}{Character scalar. Name of the (non-negative) event-count column.}

\item{exposure}{Character scalar. Name of the exposure / follow-up-time
column (strictly positive, e.g. patient-years).}

\item{covariates}{Optional character vector of baseline-covariate column
names to adjust for. \code{NULL} (the default) fits an unadjusted analysis.}

\item{car_scheme}{Character scalar passed to \code{\link[RobinCar:robincar_glm]{RobinCar::robincar_glm()}},
describing the randomization scheme used in the trial. One of \code{"simple"}
(the default), \code{"permuted-block"}, \code{"pocock-simon"}, \code{"biased-coin"}, or
\code{"urn"}. Any value other than \code{"simple"} requires \code{strata}.}

\item{strata}{Optional character vector of randomization-stratification
column names. Required when \code{car_scheme != "simple"}; when supplied it makes
the variance estimator account for the stratified randomization scheme.}

\item{reference}{Optional character scalar naming the reference treatment
level. If supplied, only comparisons of each other arm \emph{versus the
reference} are returned (rate ratio = other / reference). If \code{NULL} (the
default) all unordered pairwise comparisons are returned.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for two-sided confidence
intervals (default \code{0.05}, i.e. 95\% intervals).}

\item{x}{An object of class \code{"empirical_rate"} (or its summary).}

\item{digits}{Number of significant digits for printing.}

\item{...}{Currently ignored.}

\item{object}{An object of class \code{"empirical_rate"}.}
}
\value{
An object of class \code{"empirical_rate"}: a list with elements
\describe{
\item{\code{rates}}{Data frame of marginal rate estimates: \code{treatment},
\code{rate}, \code{se}.}
\item{\code{comparisons}}{Data frame of rate-ratio comparisons with columns
\code{comparison}, \code{group1}, \code{group2}, \code{rate_ratio}, \code{conf.low},
\code{conf.high}, \code{log_rr}, \code{se_log_rr}, \code{z}, \code{p_value}. Each rate ratio is
\code{rate(group1) / rate(group2)}.}
\item{\code{vcov_rate}}{Variance-covariance matrix of the rate estimates.}
\item{\code{log_rates}, \code{vcov_lograte}}{Log rates and their delta-method
variance-covariance matrix.}
\item{\code{robincar}}{The full \code{\link[RobinCar:robincar_glm]{RobinCar::robincar_glm()}} fit, for advanced
use.}
\item{\code{call}, \code{formula}, \code{covariates}, \code{car_scheme}, \code{strata},
\code{reference}, \code{alpha}}{Details of the call.}
}
}
\description{
Fits the empirical (distribution-free) method for comparing marginal event
rates between treatment groups when the endpoint is a count. Event counts are
transformed to subject-level rate quantities, a linear working model is fitted
with robust variance estimation via \code{\link[RobinCar:robincar_glm]{RobinCar::robincar_glm()}}, and marginal
rates are back-transformed to obtain rate ratios with confidence intervals via
the delta method.
}
\details{
For subject \eqn{j} in arm \eqn{i} the event count \eqn{Y_{ij}} and exposure
\eqn{d_{ij}} are combined into
\deqn{W_{ij} = Y_{ij} / \bar d_{i\cdot},}
where \eqn{\bar d_{i\cdot}} is the mean exposure in arm \eqn{i}. Because
\eqn{E[Y_{ij}] = d_{ij} r_i}, the arm-specific sample mean of \eqn{W_{ij}} is
exactly unbiased for the marginal rate \eqn{r_i} without any distributional
assumption on \eqn{Y_{ij}}. A linear working model \code{W ~ treatment (+ covariates)} is fitted and the resulting covariate-adjusted arm means
\eqn{\hat r_i} and their robust variance matrix are back-transformed to the
log-rate scale, where the rate ratio \eqn{\lambda_{ik} = r_i / r_k} is tested
and interval-estimated.

In the \strong{unadjusted} analysis (\code{covariates = NULL}, \code{car_scheme = "simple"}) the estimated marginal rate for each arm equals the observed
aggregate rate \eqn{\sum_j Y_{ij} / \sum_j d_{ij}} exactly, and the point
estimate of the rate ratio equals the ratio of observed rates.

\strong{Working model.} Following the reference implementation, the linear
predictor is additive in the covariates (a common-slope ANCOVA working
model). Randomization-aware variance estimation --- referred to as ANHECOVA
in the companion paper --- is obtained by setting \code{car_scheme} and \code{strata};
the working-model \emph{form} is unchanged. Treatment-by-covariate interactions
(heterogeneous slopes) are not fitted.
}
\examples{
\dontshow{if (requireNamespace("RobinCar", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(hypo_sim)

# Unadjusted analysis: estimated rates equal the observed aggregate rates.
fit <- empirical_rate(
  hypo_sim,
  treatment = "arm",
  count     = "events",
  exposure  = "exposure_years"
)
fit

# Adjusted analysis with the treatment arm oriented against a reference.
fit_adj <- empirical_rate(
  hypo_sim,
  treatment  = "arm",
  count      = "events",
  exposure   = "exposure_years",
  covariates = c("baseline_hba1c", "baseline_rate"),
  reference  = "Control"
)
summary(fit_adj)
\dontshow{\}) # examplesIf}
}
\references{
Sun J, Amoafo L, Qu Y (2026). An Empirical Method for Analyzing Count Data.
}
\seealso{
\code{\link[=meta_rate_ratio]{meta_rate_ratio()}} to pool rate ratios across strata or studies.
}
